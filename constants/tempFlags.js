// Temporary feature flags — flip to false (or delete the flag and its usages)
// once the feature it guards is no longer needed.

// Lets Prime members enter/use fertility ("მინდა დაორსულება") mode without
// buying the separate pregnancy entitlement, while its dedicated UI/pricing
// is being designed.
export const TEMP_PRIME_UNLOCKS_FERTILITY = true;
